name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build image
      run: |
        docker-compose build
        make build
    - name: Test image
      run: make test
